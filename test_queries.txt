[facts_example, scheduler].
active_workstations(morning, X).
active_workstations(evening, X).
employee_allowed(alice, morning, register).
employee_allowed(alice, morning, kitchen).
employee_allowed(carla, night, register).
employee_allowed(ella, morning, register).

[facts_test2, scheduler].
active_workstations(night, X).
employee_allowed(amy, evening, packing).
employee_allowed(brad, morning, frontdesk).

[facts_unsat, scheduler].
workstation(main, Min, Max).

[facts_example, scheduler].
select_employees_for_workstation(1, morning, register, [alice,bob,carla], Remaining, Employees).
select_employees_for_workstation(2, evening, kitchen, [alice,bob,carla,dan,ella,faris], Remaining, Employees).

[facts_example, scheduler].
active_workstations(evening, WS).
assign_workstations(WS, evening, [alice,bob,carla,dan,ella,faris], Remaining, Schedule).

[facts_example, scheduler].
build_shift_schedule(morning, [alice,bob,carla,dan,ella,faris], Remaining, Schedule).

[facts_test2, scheduler].
build_shift_schedule(evening, [amy,brad,chris,dina,eric,faiza], Remaining, Schedule).

[facts_test2, scheduler].
build_shift_schedule(night, [amy,brad,chris,dina,eric,faiza], Remaining, Schedule).

[facts_example, scheduler].
plan(P).

[facts_test2, scheduler].
plan(P).

[facts_unsat, scheduler].
plan(P).

[facts_example, scheduler].
plan(plan(Morning, Evening, Night)).